COMPOSE_PROJECT_NAME=society

# ─── Shared platform (must match the values the root .env* was started with) ──
POSTGRES_DB=society_events

# ─── user-service's own DB role — see DB_ISOLATION_PLAN.md ──
USER_DB_USER=user_role
USER_DB_PASSWORD=your_user_db_password

# ─── Keycloak ─────────────────────────────────────────────────────────────
KEYCLOAK_URL=https://auth.gm-global-techies-town.club
KEYCLOAK_PUBLIC_URL=https://auth.gm-global-techies-town.club

# ─── Inter-service auth ───────────────────────────────────────────────────
INTERNAL_API_KEY=your_internal_api_key

# ─── Service-specific ─────────────────────────────────────────────────────
APP_PUBLIC_URL=your_app_public_url
KEYCLOAK_ADMIN_USER=
KEYCLOAK_ADMIN_PASSWORD=your_keycloak_admin_password
SOCIETY_NAME=
SOCIETY_SHORT_NAME=
SOCIETY_CITY=
SPLUNK_HEC_TOKEN=your_splunk_hec_token
AUTH_SERVICE_API_KEY=your_auth_service_api_key
OTP_BRIDGE_CLIENT_ID=
OTP_BRIDGE_CLIENT_SECRET=your_otp_bridge_client_secret
GMAIL_SMTP_USER=your_gmail_smtp_user
GMAIL_APP_PASSWORD=your_gmail_app_password

# ─── PII encryption at rest (users.phone/users.email — see PII_PRIVACY_PLAN.md) ──
# PII_ENCRYPTION_KEY: base64 for exactly 32 raw bytes (AES-256-GCM). Generate with:
#   python -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
# PII_HASH_KEY: any long random secret string (HMAC-SHA256 blind index) — must be
# a DIFFERENT value from PII_ENCRYPTION_KEY. Never reuse either for anything else,
# and never commit real values here.
PII_ENCRYPTION_KEY=your_base64_32byte_pii_encryption_key
PII_HASH_KEY=your_pii_hash_key

# ─── MinIO (shared object storage — must match the root stack's values) ──
MINIO_ROOT_USER=your_minio_user
MINIO_ROOT_PASSWORD=your_minio_password
